import { SharedSubstringPipe } from './shared-substring.pipe';

describe('SharedSubstringPipe', () => {
  it('create an instance', () => {
    const pipe = new SharedSubstringPipe();
    expect(pipe).toBeTruthy();
  });
});
